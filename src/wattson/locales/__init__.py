"""Message catalogs, one JSON file per language.

A catalog is named after its language code (``de.json``, ``pt_br.json``)
and maps the English source strings to their translations; see
``wattson/i18n.py`` for the format. English ships without a catalog
because it is the source language, so this package is empty until a
translation is added.
"""
